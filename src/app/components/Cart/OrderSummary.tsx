import { Button } from '@mui/material';
import React, { useState } from 'react';

export default function OrderSummary() {
  const [count, setCount] = useState(0);

  return <div>The total cost is: 20</div>;
}
